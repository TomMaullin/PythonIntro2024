#!/usr/bin/env python

attribute2 = 'Another example attribute'

# Simple function to work out the sign of a numeric type
def minusSign(a):

  # if a is positive we output -1
  if a<0:

    return(1)

  # if a is negative we output 1
  elif a>0:

    return(-1)

  # if a is zero we output 0.
  else:

    return(0)

